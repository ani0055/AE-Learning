let drink = "Tea";
// automatically infers the already defined dataTypes

let cup = Math.random() > 0.5 ? 10 : "10";

let chaiFlavour:string = "masala chai"; // annotation
chaiFlavour = "ginger tea"

let isChai:boolean = true;

//inference means TS itselves gets the data type and 
// annotation means we have to define the value by ourself